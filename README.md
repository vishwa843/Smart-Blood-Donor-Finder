# Smart Blood Donor Finder Backend

This backend scaffold includes:
- FastAPI application entry point
- Configuration module
- Package structure for auth, database, models, schemas, routes, services, AI, notifications, utils, middleware, and core
- Uploads and tests directories

## Run locally

```bash
uvicorn app.main:app --reload
```
